# Google Cloud OAuth client (Web application type) with the Drive API enabled.
# Console: https://console.cloud.google.com -> APIs & Services -> Credentials.
# Keep the OAuth consent screen in "Testing" publishing status and add every
# collaborator's Google email as a test user (no Google review needed for up
# to 100 test users).
VITE_GOOGLE_CLIENT_ID=
